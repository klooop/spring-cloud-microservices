spring:
  application:
    #      очень важно указывать имя (ниже написано)
    name: deposit-service
  cloud:
    config:
      uri: http://config-service:8001
      fail-fast: true
      password: admin
      username: user