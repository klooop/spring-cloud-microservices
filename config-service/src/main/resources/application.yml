spring:
    cloud:
      config:
        server:
          native:
#            говорим, что другие конфигурации будут лежать в папке services
            search-locations: classpath:/services
    profiles:
      active: native
    security:
      user:
        password: admin

  server:
    port: 8001