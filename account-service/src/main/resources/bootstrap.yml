spring:
  application:
    #      очень важно указывать имя (ниже написано)
    name: account-service
  cloud:
    config:
      uri: http://localhost:8001
      fail-fast: true
      password: admin
      username: user