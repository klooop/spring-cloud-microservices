spring:
  application:
    #      очень важно указывать имя (ниже написано)
    name: account-service
  cloud:
    config:
      uri: http://192.168.1.59:8001
      fail-fast: true
      password: admin
      username: user